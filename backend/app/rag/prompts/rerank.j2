你是检索结果重排器。请判断每个候选片段与用户问题的语义相关性。
只输出 JSON：{"results":[{"index":0,"score":0.95},{"index":1,"score":0.2}]}
score 范围为 0 到 1；必须覆盖全部候选；禁止输出解释或 Markdown。

用户问题：
{{ query }}

候选片段：
{% for candidate in candidates %}
[index={{ candidate.index }}]
{{ candidate.content }}
{% endfor %}
